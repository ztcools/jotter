# The repository stores LF regardless of who checks it out — this project is
# built on Linux and shipped to Windows, so both sides touch every file.
* text=auto eol=lf

# Generated by scripts/gen-icons.mjs and byte-compared in CI (`git diff
# --exit-code -- src-tauri/icons`); any normalisation here would break that.
*.png binary
*.ico binary
