{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum surface the two Jotter webviews need: drag the mascot, pick an export path, and talk to the app's own commands. Everything else (clipboard, window geometry, tray) is driven from Rust and therefore needs no webview permission.",
  "windows": [
    "ball",
    "panel"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "dialog:allow-save"
  ]
}
